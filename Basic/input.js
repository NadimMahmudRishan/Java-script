// Run the command sequencially in vscode terminal
// 1. npm init
// 2. npm install prompt - sync

// make const for use prompt-sync.
const prompt = require("prompt-sync")()
var c
c = prompt("Size = ")
console.log(c)