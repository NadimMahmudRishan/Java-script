let Arrays = [13,23,5,3,23,45,6,7,46,90,20,100];
// The Math.max() function will not work on array.
// let higests = Math.max(Arrays);

// The Math.max() function will work on variabls or input.

var num = 200;
let higest = Math.max(13, 23, 5, 3, 23, 45, 6, 7, 46, 90, 20, 100,num)
console.log(higest);